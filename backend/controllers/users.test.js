const {
  getUsers,
  // getUserById,
  // updProfile,
  // updAvatar,
  // getCurrentUser,
  // login,
  // createUser
} = require('./users');

// eslint-disable-next-line no-undef
it('Создаёт приветствие', () => {
  // eslint-disable-next-line no-undef
  expect(getUsers()).toBe('1');
});
